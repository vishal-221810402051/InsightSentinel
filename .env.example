# =========================
# InsightSentinel AI (V1)
# =========================

# Postgres (container)
POSTGRES_DB=insightsentinel
POSTGRES_USER=insightsentinel
POSTGRES_PASSWORD=insightsentinel_pass
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DSN=postgresql+psycopg://insightsentinel:insightsentinel_pass@postgres:5432/insightsentinel

# Backend
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
LOG_LEVEL=INFO

# Optional (V1 later)
LLM_PROVIDER=none
LLM_API_KEY=